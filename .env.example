# PostgreSQL
# Local (docker compose):
DATABASE_URL=postgresql://tend:tend@localhost:5432/tend
# Neon production (Vercel) — use pooled URL for the app:
# DATABASE_URL=postgresql://user:pass@ep-xxx-pooler.region.aws.neon.tech/neondb?sslmode=require
# Neon migrations (Vercel build + optional local) — direct/unpooled URL:
# DATABASE_URL_UNPOOLED=postgresql://user:pass@ep-xxx.region.aws.neon.tech/neondb?sslmode=require

# Session signing secret — generate with: openssl rand -base64 32
SESSION_SECRET=change-me-to-a-random-32-byte-or-longer-secret

# App
NODE_ENV=development
PORT=3000

# Production canonical host — used to redirect *.vercel.app traffic (optional override)
# CANONICAL_APP_HOST=app.tend.qzz.io
# MARKETING_HOST=tend.qzz.io

# Optional manual/admin trigger for GET /api/v1/jobs/notifications.
NOTIFICATIONS_JOB_SECRET=change-me-to-a-random-token

# Firebase Cloud Messaging (server-side native push). In Vercel env vars,
# keep private-key newlines escaped as \n.
# FIREBASE_PROJECT_ID=tend-eb22b
# FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@tend-eb22b.iam.gserviceaccount.com
# FIREBASE_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n

# Pre-alpha invite list — comma-separated emails allowed to register and sign in.
# Leave unset locally to allow any email. Set in production to close the app to invitees only.
# ALLOWED_EMAILS=you@example.com,friend@example.com

# Password reset email (Resend). Leave unset locally to skip sending;
# forgot-password still succeeds without revealing whether the email exists.
# Production: set the same names in Vercel env vars (never commit the key).
# Defaults to Resend's test sender if EMAIL_FROM is unset; that sender can
# only deliver to the Resend account email. After verifying the Tend domain:
# RESEND_API_KEY=re_xxxxxxxx
# EMAIL_FROM=Tend <noreply@app.tend.qzz.io>
# APP_ORIGIN=http://localhost:3000

# Sentry — optional; leave unset locally to disable error reporting
# Web app (server + browser):
SENTRY_DSN=https://examplePublicKey@o0.ingest.sentry.io/0
NEXT_PUBLIC_SENTRY_DSN=https://examplePublicKey@o0.ingest.sentry.io/0
# Mobile app (Expo):
EXPO_PUBLIC_SENTRY_DSN=https://examplePublicKey@o0.ingest.sentry.io/0
# Source map uploads (CI / production builds only):
# SENTRY_ORG=your-org-slug
# SENTRY_PROJECT=your-project-slug
# SENTRY_AUTH_TOKEN=your-sentry-auth-token
